import { crearErrorDeNoImplementado } from "../../shared/errors/ErrorNoImplementado.js";

export const registrarse = async (datos) => {
  throw crearErrorDeNoImplementado();
};

export const logearse = async (datos) => {
  throw crearErrorDeNoImplementado();
};
